export default function RunSummary({ summaryText }) {
    return (<section className="run-section">
      <h2 className="section-title">Summary</h2>
      <p className="run-summary-text">{summaryText}</p>
    </section>);
}
